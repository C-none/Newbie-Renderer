# Test Rules

- Before modifying a module's test, read its `src/<module>/AGENTS.md` when present. Shader source, compiler, and contract tests also read `shader/AGENTS.md`.
- A focused shader compile check never justifies deleting, disabling, or skipping a registered contract, integration, smoke, or GPU-runtime test.
