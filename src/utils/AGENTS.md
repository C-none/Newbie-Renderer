# `nr.utils` Diagnostic Rules

When modifying `nr.utils:errorHandle`, preserve the public diagnostic contract in `src/AGENTS.md` and extend that facility rather than bypassing it with a parallel reporting system.
