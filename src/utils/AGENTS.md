# `nr.utils` Diagnostic Rules

When modifying `nr.utils:errorHandle`, preserve the public diagnostic contract defined in `src/AGENTS.md` `## Diagnostics and Exceptions` and extend that facility instead of adding a competing reporting path.
