export module nr.utils:staticUtilsConstants;
import std;

export namespace nr
{
inline constexpr bool isDebugMode = @IS_DEBUG_MODE@;
inline constexpr std::uint32_t maxThreads = @MAX_THREADS@u;

enum class LogLevel : std::uint32_t
{@LOG_LEVEL_ENUM@
};

inline constexpr LogLevel globalLogLevel = LogLevel::@LOG_LEVEL_NAME@;

inline constexpr std::array<std::string_view, static_cast<std::size_t>(LogLevel::number)> logLevelNames = {@LOG_LEVEL_NAMES@};

inline constexpr std::string_view shaderCacheRoot = "@NR_SHADER_CACHE_DIR@";
inline constexpr std::string_view shaderRoot = "@NR_SHADER_ROOT_DIR@";
} // namespace nr
