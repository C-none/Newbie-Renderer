cmake_minimum_required(VERSION 4.2)

# CMake still gates `import std;` behind an experimental opt-in that must be
# visible before the CXX language is enabled.
set(CMAKE_EXPERIMENTAL_CXX_IMPORT_STD "451f2fe2-a8a2-47c3-bc32-94786d8fc91b")

project(NewbieRenderer LANGUAGES CXX)

option(NR_BUILD_TESTS "Build unit tests" ON)
option(NR_ENABLE_RELEASE_IPO "Enable IPO/LTO for Release builds when supported." ON)

set(NR_LOG_LEVEL "INFO")
set(NR_SHADER_CACHE_DIR "${CMAKE_BINARY_DIR}/shader_cache" CACHE STRING "Runtime shader cache directory")
set(NR_SHADER_ROOT_DIR "${CMAKE_SOURCE_DIR}/shader" CACHE STRING "Runtime shader source root directory")

include(CheckIPOSupported)

if(CMAKE_CXX_COMPILER_ID STREQUAL "Clang" AND CMAKE_CXX_COMPILER_FRONTEND_VARIANT STREQUAL "GNU")
    set(CMAKE_CXX_MODULE_STD ON)
endif()

if(DEFINED VCPKG_CHAINLOAD_TOOLCHAIN_FILE AND VCPKG_CHAINLOAD_TOOLCHAIN_FILE MATCHES "msys2-clangxx\\.cmake$")
    if(NOT CMAKE_CXX_COMPILER_ID STREQUAL "Clang")
        message(FATAL_ERROR "The MSYS2 clang++ toolchain preset requires clang++ as the active C++ compiler.")
    endif()

    if(NOT CMAKE_CXX_COMPILER_FRONTEND_VARIANT STREQUAL "GNU")
        message(FATAL_ERROR "The MSYS2 clang++ toolchain preset requires the GNU-style clang++ frontend.")
    endif()

    if(NOT ";${CMAKE_CXX_COMPILER_IMPORT_STD};" MATCHES ";23;")
        message(FATAL_ERROR
            "The MSYS2 clang++ toolchain preset requires import std support for C++23. "
            "Verify libc++.modules.json is discoverable by CMake."
        )
    endif()
endif()

find_package(Vulkan REQUIRED)
find_package(imgui CONFIG REQUIRED)
find_package(glm REQUIRED)
find_package(glfw3 CONFIG REQUIRED)
find_package(VulkanMemoryAllocator CONFIG REQUIRED)
find_package(tracy CONFIG REQUIRED)
find_package(assimp CONFIG REQUIRED)
find_package(Stb REQUIRED)
find_package(libjpeg-turbo CONFIG REQUIRED)

if(${Vulkan_VERSION} VERSION_LESS "1.4.341")
    message(FATAL_ERROR "Minimum required Vulkan version for C++ modules is 1.4.341. "
        "Found ${Vulkan_VERSION}."
    )
endif()

# add_library(VulkanHppModule)
# target_sources(VulkanHppModule PRIVATE
#     FILE_SET CXX_MODULES
#     BASE_DIRS ${Vulkan_INCLUDE_DIR}
#     FILES ${Vulkan_INCLUDE_DIR}/vulkan/vulkan.cppm
# )
# target_compile_features(VulkanHppModule PUBLIC cxx_std_23)
# target_link_libraries(VulkanHppModule PUBLIC Vulkan::Vulkan)

if(NR_ENABLE_RELEASE_IPO)
    check_ipo_supported(RESULT NR_RELEASE_IPO_SUPPORTED OUTPUT NR_RELEASE_IPO_OUTPUT LANGUAGES CXX)
else()
    set(NR_RELEASE_IPO_SUPPORTED FALSE)
endif()

function(nr_apply_target_settings target)
    if(MSVC)
        # Modern parallel compilation
        target_compile_options(${target} PRIVATE /MP /EHsc)
        target_compile_definitions(${target} PRIVATE
            $<$<CONFIG:Debug>:_ITERATOR_DEBUG_LEVEL=2>
            $<$<NOT:$<CONFIG:Debug>>:_ITERATOR_DEBUG_LEVEL=0>
        )

        if(NR_RELEASE_IPO_SUPPORTED)
            set_target_properties(${target} PROPERTIES
                INTERPROCEDURAL_OPTIMIZATION_RELEASE TRUE
            )
        endif()

        target_compile_options(${target} PRIVATE
            $<$<CONFIG:Debug>:/W4 /WX /Zi>
            $<$<CONFIG:Release>:/W4 /WX /Ox>
        )

        target_link_options(${target} PRIVATE
            $<$<CONFIG:Debug>:/INCREMENTAL /DEBUG:FULL>
            $<$<CONFIG:Release>:/INCREMENTAL:NO /OPT:REF /OPT:ICF>
        )
    elseif(CMAKE_CXX_COMPILER_ID STREQUAL "Clang" AND CMAKE_CXX_COMPILER_FRONTEND_VARIANT STREQUAL "GNU")
        if(NR_RELEASE_IPO_SUPPORTED)
            set_target_properties(${target} PROPERTIES
                INTERPROCEDURAL_OPTIMIZATION_RELEASE TRUE
            )
        endif()

        target_compile_options(${target} PRIVATE
            -Wall
            -Wextra
            -Werror
            $<$<CONFIG:Debug>:-Og -g3 -fno-omit-frame-pointer>
            $<$<CONFIG:Release>:-O3>
        )

        target_link_options(${target} PRIVATE
            $<$<CONFIG:Release>:-Wl,--gc-sections -Wl,--icf=all>
        )
    endif()
endfunction()

function(nr_add_executable target)
    add_executable(${target} ${ARGN})
    nr_apply_target_settings(${target})
endfunction()

function(nr_add_library target)
    add_library(${target} ${ARGN})
    nr_apply_target_settings(${target})
endfunction()

function(nr_add_clang_module_object_depends source_file)
    if(NOT (CMAKE_CXX_COMPILER_ID STREQUAL "Clang" AND CMAKE_CXX_COMPILER_FRONTEND_VARIANT STREQUAL "GNU"))
        return()
    endif()

    set(module_object_depends "")
    foreach(dep_target IN LISTS ARGN)
        if(NOT TARGET ${dep_target})
            continue()
        endif()

        get_target_property(dep_target_type ${dep_target} TYPE)
        if(NOT dep_target_type STREQUAL "STATIC_LIBRARY")
            continue()
        endif()

        get_target_property(dep_target_binary_dir ${dep_target} BINARY_DIR)
        list(APPEND module_object_depends "${dep_target_binary_dir}/${CMAKE_STATIC_LIBRARY_PREFIX}${dep_target}${CMAKE_STATIC_LIBRARY_SUFFIX}")
    endforeach()

    if(module_object_depends)
        set_source_files_properties(${source_file} PROPERTIES OBJECT_DEPENDS "${module_object_depends}")
    endif()
endfunction()

add_subdirectory(src)
add_subdirectory(shader)

if(NR_BUILD_TESTS)
    enable_testing()
    # Configure CTest output and ensure Testing directory structure
    set(CTEST_OUTPUT_ON_FAILURE ON CACHE BOOL "Enable verbose CTest output on failure" FORCE)
    file(MAKE_DIRECTORY "${CMAKE_BINARY_DIR}/Testing/Temporary")

    add_subdirectory(test)
endif()

# Copy compile_commands.json to the root directory for clangd
# add_custom_target(copy_compile_commands ALL
#     COMMAND ${CMAKE_COMMAND} -E copy_if_different
#         "${CMAKE_BINARY_DIR}/compile_commands.json"
#         "${CMAKE_SOURCE_DIR}/compile_commands.json"
#     COMMENT "Copying compile_commands.json to root directory"
# )
