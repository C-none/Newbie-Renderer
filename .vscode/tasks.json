{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "CMake: configure llvm-debug",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--preset",
        "llvm-debug"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "CMake: configure llvm-release",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--preset",
        "llvm-release"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "CMake: build debug main",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "--preset",
        "debug",
        "--target",
        "main"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "dependsOn": "CMake: configure llvm-debug",
      "problemMatcher": [
        "$gcc"
      ]
    },
    {
      "label": "CMake: build release main",
      "type": "shell",
      "command": "cmake",
      "args": [
        "--build",
        "--preset",
        "release",
        "--target",
        "main"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "dependsOn": "CMake: configure llvm-release",
      "problemMatcher": [
        "$gcc"
      ]
    }
  ]
}
